import React from 'react';
import { View, Text } from 'react-native';

const Player: React.FC = () => {
    return (
        <View>
            <Text>On Native</Text>
        </View>
    );
};

export default Player;

import React from 'react';
import { View, Text } from 'react-native';

const Player: React.FC = () => {
    return (
        <View>
            <Text>On Web</Text>
        </View>
    );
};

export default Player;

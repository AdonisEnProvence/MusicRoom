import { styled } from 'dripsy';
import { View } from 'react-native';

const FlexColContainer = styled(View)(() => ({
    flexDirection: 'column',
    justifyContent: 'center',
}));

export default FlexColContainer;

{
    "name": "@musicroom/temporal",
    "version": "0.0.1",
    "dependencies": {},
    "volta": {
        "node": "16.0.0",
        "yarn": "1.22.10"
    },
    "scripts": {
        "dev": "nodemon --signal SIGTERM --exec env PORT=3000 ADONIS_ENDPOINT='http://localhost:3333' go run api/main.go",
        "worker": "nodemon --signal SIGTERM --exec env PORT=3000 ADONIS_ENDPOINT=http://localhost:3333 go run worker/main.go",
        "temporal": "cd docker-compose && docker-compose up -d",
        "test": "go test ./..."
    },
    "devDependencies": {
        "nodemon": "^2.0.7"
    }
}
